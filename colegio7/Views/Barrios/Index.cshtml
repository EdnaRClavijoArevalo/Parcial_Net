@model IEnumerable<colegio7.Models.Barrios>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreBarrio)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreBarrio)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idBarrio }) |
                @Html.ActionLink("Details", "Details", new { id=item.idBarrio }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idBarrio })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
